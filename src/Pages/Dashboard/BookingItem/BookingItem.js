import React from 'react';

const BookingItem = ({bikeName, price,byreName,email}) => {
    return (
        <div>
            <h1>{bikeName}</h1>
        </div>
    );
};

export default BookingItem;